import { Component } from '@angular/core';

/**
 * A small styled component, used to demonstrate what each encapsulation mode
 * does to its styles.
 */
@Component({
  selector: 'app-ve-badge',
  styleUrl: './ve-badge.css',
  templateUrl: './ve-badge.html',
})
export class VeBadge {}
