import { Component } from '@angular/core';

/** Demonstrates ngProjectAs — projects ng-container as a specific selector */
@Component({
  selector: 'app-badge-host',
  standalone: true,
  templateUrl: './badge-host.html',
  styleUrl: './badge-host.css',
})
export class BadgeHost {}
