/** Straggler fix: lengthen one distractor so the correct option is no longer the
 * unique longest. Answer text and letter references unchanged. */
export default {
  315: { answer: 1, options: [
    `They are functionally identical, so the choice is purely cosmetic`,
    `Compile-time bakes per-locale bundles; runtime swaps JSON live`,
    `ngx-translate is inherently faster because it skips the translation step entirely`,
    `Built-in i18n supports only two locales per application`,
  ] },
};
